apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  annotations:
    kubectl.kubernetes.io/last-applied-configuration: |
  name: httproute-argoserver-443
  namespace: cicd-tools
spec:
  hostnames:
  - argocd-md-dev.ktcloud.com
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: gateway-admindev
    namespace: nginx-gateway
    sectionName: https-ktcloud-com
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: argocd-server
      port: 80
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /

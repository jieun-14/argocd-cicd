apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: ByoMachineTemplate
metadata:
  name: ${CLUSTER_NAME}-md-0
  namespace: ${NAMESPACE}
spec:
  template:
    spec:
      selector:
        matchLabels:
          role: ${COMPUTE_NODE_ROLE}
          environment: ${ENVIRONMENT}

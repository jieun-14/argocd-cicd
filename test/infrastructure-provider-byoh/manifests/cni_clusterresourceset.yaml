apiVersion: addons.cluster.x-k8s.io/v1beta1
kind: ClusterResourceSet
metadata:
  name: ${CNI_NAME}
  namespace: ${NAMESPACE}
spec:
  clusterSelector:
    matchLabels:
      cni: ${CNI_LABEL}
  resources:
  - kind: ConfigMap
    name: kube-ovn-configmap
  strategy: ApplyOnce
